# CONFIGURATION — ARTCB MVP Avancé

**Horodatage :** 2026-07-04T20:25:00Z  
**Statut :** Décisions utilisateur intégrées — voir DECISIONS_UTILISATEUR_ARTCB

---

## 1. Variables d'environnement requises

| Variable | Obligatoire | Défaut | Description |
|----------|-------------|--------|-------------|
| `ARTCB_DEBUG` | Non | `true` | Mode DEBUG (PROTOCOLE : actif par défaut) |
| `ARTCB_LOG_LEVEL` | Non | `DEBUG` | Niveau logs : DEBUG, INFO, WARNING, ERROR |
| `ARTCB_LOG_DIR` | Non | `./logs` | Dossier logs JSON |
| `ARTCB_REPORTS_DIR` | Non | `./rapports` | Dossier rapports |
| `ARTCB_DATA_DIR` | Non | `./data` | Données persistantes (chaîne, graphes) |
| `ARTCB_HOST` | Non | `0.0.0.0` | Host API FastAPI |
| `ARTCB_PORT` | Non | `8000` | Port API |
| `ARTCB_CHAIN_KEY_PATH` | Non | `./data/chain.key` | Clé Ed25519 chaîne privée |
| `ARTCB_ENCRYPTION_KEY` | Non | auto-généré | Clé AES-256-GCM (fichier local) |
| `ARTCB_POL_ALPHA` | Non | `0.4` | Poids Δcompression dans PoL |
| `ARTCB_POL_BETA` | Non | `0.3` | Poids validation_rate |
| `ARTCB_POL_GAMMA` | Non | `0.3` | Poids retrieval_accuracy |
| `ARTCB_POL_THRESHOLD` | Non | `0.6` | Seuil acceptation bloc |
| `OPENROUTER_API_KEY` | Non* | — | Clé LLM pour encodage intelligent |
| `OPENROUTER_MODEL` | Non | `anthropic/claude-3.5-sonnet` | Modèle LLM |
| `GRADIUM_API_KEY` | Non | — | TTS voix (P1 hackathon) |
| `GRADIUM_API_URL` | Non | `https://api.gradium.ai` | Base URL Gradium |

\* Obligatoire seulement si encodage LLM activé ; sinon fallback rule-based (CDC §21).

---

## 2. Fichier `.env.example` (à créer en Phase 1)

```bash
# ARTCB Configuration — copier vers .env et remplir
ARTCB_DEBUG=true
ARTCB_LOG_LEVEL=DEBUG
ARTCB_LOG_DIR=./logs
ARTCB_REPORTS_DIR=./rapports
ARTCB_DATA_DIR=./data
ARTCB_HOST=0.0.0.0
ARTCB_PORT=8000
ARTCB_CHAIN_KEY_PATH=./data/chain.key
ARTCB_POL_ALPHA=0.4
ARTCB_POL_BETA=0.3
ARTCB_POL_GAMMA=0.3
ARTCB_POL_THRESHOLD=0.6

# Optionnel — encodage LLM
# OPENROUTER_API_KEY=sk-or-v1-...
# OPENROUTER_MODEL=anthropic/claude-3.5-sonnet

# Optionnel — voix P1
# GRADIUM_API_KEY=...
```

**Sécurité :** `.env` doit être dans `.gitignore` — jamais commité.

---

## 3. Dépendances Python (requirements.txt — spec)

```
# Core
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
python-multipart>=0.0.9

# Graphe & mémoire
networkx>=3.3
numpy>=1.26.0
faiss-cpu>=1.8.0

# Crypto
cryptography>=42.0.0
pynacl>=1.5.0

# Utilitaires
python-dotenv>=1.0.0
httpx>=0.27.0
websockets>=12.0

# NLP fallback (sans LLM)
spacy>=3.7.0
# python -m spacy download fr_core_news_sm

# Tests
pytest>=8.2.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0

# Qualité
ruff>=0.4.0
mypy>=1.10.0
```

---

## 4. Dépendances Frontend (package.json — spec)

```json
{
  "dependencies": {
    "react": "^18.3.0",
    "react-dom": "^18.3.0",
    "cytoscape": "^3.28.0",
    "cytoscape-react": "^2.0.0",
    "axios": "^1.7.0"
  },
  "devDependencies": {
    "vite": "^5.3.0",
    "@vitejs/plugin-react": "^4.3.0",
    "typescript": "^5.5.0",
    "@types/react": "^18.3.0"
  }
}
```

---

## 5. Dépendances système

| Outil | Version min | Usage |
|-------|-------------|-------|
| Python | 3.11+ | Backend |
| Node.js | 20+ | Frontend |
| GCC / Clang | C11 | libartcb_chain (Phase 3) |
| Make | — | Compilation C |
| Docker | 24+ | Démo reproductible (optionnel) |
| Git | 2.40+ | Versionnement |

---

## 6. Ports réseau

| Service | Port | Protocole |
|---------|------|-----------|
| API FastAPI | 8000 | HTTP |
| WebSocket | 8000/ws | WS |
| Frontend Vite dev | 5173 | HTTP |
| Frontend prod | 3000 | HTTP (via nginx/docker) |

---

## 7. Structure données locales (`ARTCB_DATA_DIR`)

```
data/
├── chain.key              # Clé Ed25519 (auto-générée)
├── encryption.key         # Clé AES (auto-générée)
├── chain/
│   ├── blocks.jsonl       # Blocs blockchain
│   └── index.db           # SQLite index (optionnel)
├── graphs/
│   └── {uuid}.json        # Graphes IR persistés
├── rtleg/
│   └── events.jsonl       # Journal RT-LEG
└── vectors/
    └── {graph_id}.faiss   # Index vectoriel par graphe
```

---

## 8. Configuration Docker Compose (spec)

```yaml
services:
  api:
    build: .
    ports: ["8000:8000"]
    volumes: ["./data:/app/data", "./logs:/app/logs"]
    env_file: .env
  frontend:
    build: ./frontend
    ports: ["5173:5173"]
    depends_on: [api]
```

---

## 9. Clés API — clarification (blockchain décentralisée)

### Principe

**La blockchain ARTCB (C) ne contient et ne demande AUCUNE clé API.**

Les clés ci-dessous sont **optionnelles**, pour services **externes** uniquement, stockées **localement** dans `.env` (gitignoré, jamais commitées, jamais sur la chaîne).

| Variable | Service externe | Obligatoire ? | Sur blockchain ? |
|----------|----------------|---------------|------------------|
| `OPENROUTER_API_KEY` | IA cloud (encodage LLM) | Non — rule-based par défaut | ❌ Jamais |
| `GRADIUM_API_KEY` | Synthèse vocale hackathon | Non — skip par défaut | ❌ Jamais |

### Où vont les clés si tu en fournis ?

```
lvx/
├── .env              ← TON fichier local (gitignoré)
├── .env.example      ← Modèle sans secrets (dans le repo)
├── data/chain/       ← Blockchain C (open source, pas de clés)
└── src/c/            ← Core blockchain C
```

Chaque clone du repo public fonctionne **sans clé**. Les clés sont un choix **personnel local** pour activer des bonus (LLM, voix).

### Éléments utilisateur — statut

| Élément | Statut | Responsable |
|---------|--------|-------------|
| Validation CDC v1.1 | ✅ | Agent |
| Phase 1 IR Engine | ✅ | Agent |
| `OPENROUTER_API_KEY` | ⏳ Q-002 en attente | Toi (optionnel) |
| `GRADIUM_API_KEY` | ⏳ Q-004 en attente | Toi (optionnel) |
| Équipe hackathon | Hors scope agent | Toi |
| Merge → main à chaque push | ✅ Règle D-001 | Agent |
| Crédits Cursor formulaire | ⏳ | Toi |
