# ARTCB Configuration — copy to .env and fill (NEVER commit .env)
ARTCB_DEBUG=true
ARTCB_LOG_LEVEL=DEBUG
ARTCB_LOG_DIR=./logs
ARTCB_REPORTS_DIR=./rapports
ARTCB_DATA_DIR=./data

# Priority A: rule-based (always available — Q-002 = both paths)
ARTCB_ENCODE_MODE=rule-based
ARTCB_LLM_ENABLED=false

# Priority B: IBM Bob CLI — NOT OpenRouter from hackathon (Q-002 + Q-009 D)
# BOB_API_KEY=bob_prod_...
# OPENROUTER_API_KEY=bob_prod_...   # optional alias — same Bob key
# BOB_API_BASE=https://api.us-east.bob.ibm.com
# ARTCB_LLM_ENABLED=true

# Demo book — Wailly 100% (Q-006)
ARTCB_DEMO_BOOK_PDF=data/fixtures/wailly_le_roi_de_l_inconnu.pdf

# Gradium TTS (optional UI voice)
# GRADIUM_API_KEY=gsk_...
# GRADIUM_API_URL=https://api.gradium.ai

# GitHub PAT (optional HTTPS fallback — SSH preferred)
# GITHUB_TOKEN=ghp_...
